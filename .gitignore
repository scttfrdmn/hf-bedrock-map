# Local refresh output — written to repo root during `go run ./cmd/refresh`.
# The committed copies live under docs/ (written by CI with OUTPUT_DIR=docs).
/mapping.json
/api/

# Go build artifacts
/refresh
/cmd/refresh/refresh

# Editor / OS cruft
.DS_Store

# Local secrets (HF_TOKEN etc.) — never commit
.env

# Original spec archive (contents now live in README.md / CLAUDE.md)
hf-bedrock-map.zip
